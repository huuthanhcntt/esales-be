# Local dev — uses localhost with Docker-exposed ports
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/reservations
PORT=4000

AUTH_HOST=localhost
AUTH_PORT=3002
PAYMENTS_HOST=localhost
PAYMENTS_PORT=3003

# Optional — remove to use TCP transport / in-memory cache
KAFKA_BROKER=localhost:9093
REDIS_URL=redis://localhost:6380