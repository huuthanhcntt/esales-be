# Local dev — uses localhost with Docker-exposed ports
PORT=3003

NOTIFICATIONS_HOST=localhost
NOTIFICATIONS_PORT=3004

STRIPE_STUB=true

# Optional — remove to use TCP transport
KAFKA_BROKER=localhost:9093